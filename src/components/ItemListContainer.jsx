export function ItemListContainer({ title, subtitle }) {
  return (
    <div className="container">
      <h1 className="title">{title}</h1>
      <h2 className="subtitle">{subtitle}</h2>
    </div>
  );
}
